// * React and Redux:
import React from 'react';

// * MUI:
import {
    Stack 
} from '@mui/material';

const Header = () => {
    return (
        <Stack className="header">
            Header
        </Stack>
    )
}

export default Header;